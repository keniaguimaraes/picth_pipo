
<%= form_with(model: cliente) do |form| %>
  <% if cliente.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% cliente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


    <div class="item form-group<%= @cliente.errors[:nome].any? ? ' has-error has-feedback' : '' %>">
    <%= form.label :nome, "Nome: " %>
      <div class="input-group">
        <%= form.text_field :nome, {:class=> "form-control", :placeholder=>"Nome do Cliente"} %>        
        <%if @cliente.errors[:nome].any? %>
          <p class="bg-danger"><%= @cliente.errors[:nome].to_s[/\w([^"]*)/] %></p>
        <% end %>
      </div>
    </div>

    <div class="actions col-md-9 col-sm-9 col-xs-12 col-md-offset-3">
      <%= form.submit @cliente.new_record? ? "Adicionar" : "Atualizar", :class => "btn btn-primary"%>
    </div>  

    <% end %>
