<p id="notice"><%= notice %></p>
<div>  
   <%= link_to "Adicionar", new_funcionario_path, "data-controller": "turbo",class: 'btn btn-primary' %>
</div>

<br />
<div class="card shadow mb-4">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered" id="tablacliente" width="100%" cellspacing="0">
                <thead>
                <tr>
                    <th>Nome</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                    <% @funcionarios.each do |funcionario| %>
                      <tr id="funcionario_<%= funcionario.id %>">
                        <td><%= funcionario.nome %></td>
                        <td>
                          <%= link_to "Benefícios", funcionariobeneficios_path(funcionario_id:funcionario.id), class: 'btn btn-info btn-sm' %>
                          <%= link_to "Editar", edit_funcionario_path(funcionario), "data-controller": "turbo", class: 'btn btn-warning btn-sm' %>
                          <%= link_to "Deletar", funcionario, class: 'btn btn-danger btn-sm',
                                  "data-turbo-method": :delete,
                                  "data-turbo-confirm": "Tem certeza?" %>
                        </td>
                    </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
      </div>
     
     <div class="card-footer">
          <div class="s12 m12 lg12">
               <%= will_paginate @diarios,renderer: BootstrapPagination::Rails %> </div>
          </div>
     </div>
</div>
